# GreenHub3d

Платформа для обмена 3D моделями - пародия Sketchfab.

## Возможности

- 🔐 Регистрация и вход пользователей
- 📤 Загрузка 3D моделей (только для авторизованных пользователей)
- 👤 Профили пользователей с возможностью редактирования
- 🏷️ Система категорий (пользователи могут создавать категории)
- ❤️ Лайки моделей (один лайк на модель, как в YouTube)
- 👥 Подписка на других пользователей
- 📄 Лицензии: CC0, CC4, Attribution, Non-commercial
- 🎬 3D просмотрщик с поддержкой анимаций (Three.js)
- ⬇️ Скачивание моделей в оригинальном формате
- 🔍 Поиск моделей
- 📊 Статистика (просмотры, лайки, скачивания)

## Технологии

### Backend
- Node.js + Express
- MongoDB + Mongoose
- JWT для аутентификации
- Multer для загрузки файлов

### Frontend
- React
- React Router
- Three.js + React Three Fiber для 3D просмотра
- Axios для API запросов

## Установка

### Требования
- Node.js (v16 или выше)
- MongoDB (локально или MongoDB Atlas)

### Шаги установки

1. Клонируйте репозиторий
```bash
git clone <repository-url>
cd Sketchfabclone
```

2. Установите зависимости
```bash
npm run install-all
```

3. Настройте переменные окружения

Создайте файл `server/.env`:
```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/greenhub3d
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRE=7d
NODE_ENV=development
```

4. Запустите MongoDB (если используете локально)
```bash
# Windows (если MongoDB установлен)
mongod

# Mac/Linux
sudo systemctl start mongod
# или
mongod --dbpath ~/data/db
```

5. Запустите приложение
```bash
# Запуск backend и frontend одновременно
npm run dev

# Или отдельно:
npm run server  # Backend на http://localhost:5000
npm run client  # Frontend на http://localhost:3000
```

## Использование

1. Откройте браузер и перейдите на `http://localhost:3000`
2. Зарегистрируйтесь или войдите
3. Создайте категорию (опционально)
4. Загрузите свою 3D модель
5. Просматривайте и скачивайте модели других пользователей

## Поддерживаемые форматы моделей

- GLTF/GLB (рекомендуется для веб-просмотра)
- FBX
- OBJ
- 3DS
- DAE
- BLEND

## Структура проекта

```
Sketchfabclone/
├── server/              # Backend
│   ├── models/          # MongoDB модели
│   ├── routes/          # API маршруты
│   ├── middleware/      # Middleware (auth)
│   ├── utils/           # Утилиты
│   └── uploads/         # Загруженные файлы
├── client/              # Frontend
│   ├── src/
│   │   ├── components/  # React компоненты
│   │   ├── pages/       # Страницы
│   │   ├── context/     # Context API
│   │   └── App.jsx      # Главный компонент
│   └── public/          # Статические файлы
└── package.json         # Корневой package.json
```

## API Endpoints

### Auth
- POST `/api/auth/register` - Регистрация
- POST `/api/auth/login` - Вход

### Users
- GET `/api/users/:id` - Получить профиль
- PUT `/api/users/:id` - Обновить профиль
- POST `/api/users/:id/follow` - Подписаться
- POST `/api/users/:id/unfollow` - Отписаться

### Models
- GET `/api/models` - Список моделей
- GET `/api/models/:id` - Детали модели
- POST `/api/models` - Загрузить модель (требуется авторизация)
- PUT `/api/models/:id` - Обновить модель
- DELETE `/api/models/:id` - Удалить модель
- POST `/api/models/:id/like` - Лайкнуть модель
- POST `/api/models/:id/unlike` - Убрать лайк
- GET `/api/models/:id/download` - Скачать модель

### Categories
- GET `/api/categories` - Список категорий
- GET `/api/categories/:id` - Детали категории
- POST `/api/categories` - Создать категорию (требуется авторизация)

## Лицензия

MIT

## Примечания

- Максимальный размер файла: 100MB
- Для лучшей производительности рекомендуется использовать GLTF/GLB форматы
- Анимации поддерживаются только в GLTF/GLB файлах
